# Legacy Scripts

These scripts were archived because they no longer represent the supported workflow.

They are preserved for reference only. Reasons they are not authoritative anymore:

- they deploy only one side of the stack
- they target older attachment experiments
- they do not reflect the supported hybrid HID-child architecture

Use the scripts in [scripts](../../scripts) instead.
