# Archive

Only paused, unsupported, or unrelated experiments belong here.

Rules:

- do not place active source-of-truth implementation here
- do not use archive paths as build inputs
- prefer deleting generated artifacts instead of archiving them
