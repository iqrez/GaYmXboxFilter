#pragma once

/*
 * The lower filter shares the same ABI contract as the upper filter.
 * Keep this wrapper so existing include paths remain stable while the
 * authoritative protocol lives in the repository root.
 */

#include "..\ioctl.h"
